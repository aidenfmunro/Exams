# Билет 19. Случайные последовательности с независимыми приращениями. Блуждания

Источники: Ширяев 2; Сердобольская, лекции 2-3.

## 1. Короткий ответ

Случайная последовательность $(S_n)_{n\ge0}$ имеет независимые приращения, если для любых

$$
0\le n_0<n_1<\cdots<n_k
$$

случайные величины

$$
S_{n_1}-S_{n_0},\quad S_{n_2}-S_{n_1},\quad\ldots,\quad S_{n_k}-S_{n_{k-1}}
$$

независимы.

Главный дискретный пример - случайное блуждание:

$$
S_0=0,
\qquad
S_n=\xi_1+\cdots+\xi_n,
$$

где $\xi_1,\xi_2,\ldots$ - независимые случайные величины. Если шаги одинаково распределены, то приращения не только независимы, но и стационарны:

$$
S_{n+k}-S_n\stackrel d=S_k.
$$

Для iid-шагов характеристическая функция суммы:

$$
\varphi_{S_n}(t)=\left(\varphi_\xi(t)\right)^n.
$$

Если $\mathbb E\xi_1=a$ и $\operatorname{Var}\xi_1=\sigma^2$, то

$$
\mathbb ES_n=na,
\qquad
\operatorname{Var}S_n=n\sigma^2.
$$

## 2. Полный ответ

### 19.1. Определение независимых приращений

Этот билет продолжает [[16|Билет 16]] о случайных последовательностях и связан с [[09|Билетом 09]] о независимости. Здесь важный объект не просто последовательность независимых величин, а процесс, у которого независимы изменения на непересекающихся промежутках времени.

Пусть

$$
(S_n)_{n\ge0}
$$

- случайная последовательность со значениями в $\mathbb R$, $\mathbb Z$ или $\mathbb R^d$. Приращением за промежуток от $m$ до $n$ называется

$$
S_n-S_m,
\qquad
0\le m<n.
$$

Последовательность имеет независимые приращения, если для любых моментов времени

$$
0\le n_0<n_1<\cdots<n_k
$$

приращения по непересекающимся промежуткам

$$
S_{n_1}-S_{n_0},\quad
S_{n_2}-S_{n_1},\quad\ldots,\quad
S_{n_k}-S_{n_{k-1}}
$$

независимы.

Важно: независимые приращения не означают, что сами значения $S_0,S_1,S_2,\ldots$ независимы. Наоборот, значения обычно сильно зависимы, потому что

$$
S_{n+1}=S_n+(S_{n+1}-S_n).
$$

Например, в случайном блуждании $S_{10}$ содержит в себе все шаги, которые входят в $S_9$.

### 19.2. Стационарные приращения

Если распределение приращения зависит только от длины промежутка, то говорят о стационарных приращениях:

$$
S_{n+k}-S_n\stackrel d=S_k-S_0.
$$

Если обычно берут $S_0=0$, то:

$$
S_{n+k}-S_n\stackrel d=S_k.
$$

### 19.3. Конструкция процесса через суммы независимых величин

Главная конструкция дискретного процесса с независимыми приращениями - сумма независимых шагов. Пусть

$$
\xi_1,\xi_2,\ldots
$$

- независимые случайные величины. Определим

$$
S_0=0,
\qquad
S_n=\sum_{j=1}^n\xi_j.
$$

Тогда

$$
S_n-S_m=\sum_{j=m+1}^n\xi_j.
$$

Если промежутки времени не пересекаются, то соответствующие суммы состоят из непересекающихся наборов независимых величин $\xi_j$. Поэтому такие приращения независимы.

Если $\xi_j$ одинаково распределены, то приращение

$$
S_{n+k}-S_n=\xi_{n+1}+\cdots+\xi_{n+k}
$$

имеет то же распределение, что

$$
S_k=\xi_1+\cdots+\xi_k.
$$

Значит при iid-шагах процесс имеет независимые и стационарные приращения.

### 19.4. Характеристические функции и моменты

Распределение суммы выражается через свертку. Если шаги iid с законом $\mu$, то

$$
S_n\sim \mu^{*n},
$$

где $\mu^{*n}$ - $n$-кратная свертка меры $\mu$. Через характеристические функции это особенно просто. Пусть

$$
\varphi_\xi(t)=\mathbb E e^{it\xi_1}.
$$

Тогда из независимости:

$$
\varphi_{S_n}(t)
=
\mathbb E e^{it(\xi_1+\cdots+\xi_n)}
=
\prod_{j=1}^n\mathbb E e^{it\xi_j}
=
\left(\varphi_\xi(t)\right)^n.
$$

Для не одинаково распределенных независимых шагов:

$$
\varphi_{S_n}(t)=\prod_{j=1}^n\varphi_{\xi_j}(t).
$$

Если существуют первые два момента, то они складываются:

$$
\mathbb ES_n
=
\sum_{j=1}^n\mathbb E\xi_j,
$$

и при независимости

$$
\operatorname{Var}S_n
=
\sum_{j=1}^n\operatorname{Var}\xi_j.
$$

В iid-случае, если

$$
\mathbb E\xi_1=a,
\qquad
\operatorname{Var}\xi_1=\sigma^2,
$$

получаем

$$
\mathbb ES_n=na,
\qquad
\operatorname{Var}S_n=n\sigma^2.
$$

### 19.5. Случайное блуждание

Случайное блуждание - это процесс вида

$$
S_n=S_0+\xi_1+\cdots+\xi_n,
$$

обычно с $S_0=0$ или заданным начальным состоянием. Если шаги принимают значения в $\mathbb Z$, то блуждание идет по целочисленной решетке. Самый классический пример - простое блуждание на $\mathbb Z$:

$$
P\{\xi_k=1\}=p,
\qquad
P\{\xi_k=-1\}=q=1-p.
$$

Если

$$
p=q=\frac12,
$$

блуждание называется простым симметричным.

Для простого блуждания

$$
S_n=\xi_1+\cdots+\xi_n.
$$

Если за $n$ шагов было $r$ шагов вправо и $n-r$ шагов влево, то

$$
S_n=r-(n-r)=2r-n.
$$

Поэтому

$$
P\{S_n=x\}
=
\binom n{(n+x)/2}p^{(n+x)/2}q^{(n-x)/2},
$$

если $n+x$ четно и $|x|\le n$; иначе вероятность равна $0$.

Для простого блуждания:

$$
\mathbb E\xi_1=p-q=2p-1,
$$

$$
\operatorname{Var}\xi_1=1-(p-q)^2=4pq.
$$

Следовательно,

$$
\mathbb ES_n=n(p-q),
\qquad
\operatorname{Var}S_n=4npq.
$$

В симметричном случае:

$$
\mathbb ES_n=0,
\qquad
\operatorname{Var}S_n=n.
$$

### 19.6. Марковское свойство и асимптотика

Случайное блуждание является марковской цепью. Если текущее состояние равно $i$, то следующее состояние равно

$$
i+\xi_{n+1}.
$$

Будущий шаг независим от прошлого, поэтому условное распределение будущего зависит только от текущего состояния. Для простого блуждания на $\mathbb Z$ переходные вероятности:

$$
p_{i,i+1}=p,
\qquad
p_{i,i-1}=q,
$$

а остальные переходы равны нулю. Это связывает билет с [[22|Билетом 22]] и [[23|Билетом 23]].

Случайное блуждание обычно не является стационарным по значениям: распределение $S_n$ меняется с $n$, дисперсия растет как $n$. Но при iid-шагах оно имеет стационарные приращения. Это типичная ловушка: стационарные приращения не равны стационарности самого процесса.

Асимптотически блуждание связано с законом больших чисел и центральной предельной теоремой. Если шаги iid и имеют конечное среднее $a$, то

$$
\frac{S_n}{n}\xrightarrow{P} a
$$

по закону больших чисел. Если дисперсия $\sigma^2>0$ конечна, то

$$
\frac{S_n-na}{\sigma\sqrt n}
\Rightarrow
N(0,1).
$$

Это связь с [[21|Билетом 21]].

## 3. Основные определения

### Приращение

Для последовательности $(S_n)$ приращение на промежутке $(m,n]$:

$$
S_n-S_m.
$$

### Независимые приращения

Последовательность имеет независимые приращения, если для любых

$$
0\le n_0<n_1<\cdots<n_k
$$

величины

$$
S_{n_1}-S_{n_0},\ldots,S_{n_k}-S_{n_{k-1}}
$$

независимы.

### Стационарные приращения

Распределение приращения зависит только от длины промежутка:

$$
S_{n+k}-S_n\stackrel d=S_k-S_0.
$$

Если $S_0=0$, то

$$
S_{n+k}-S_n\stackrel d=S_k.
$$

### Случайное блуждание

Процесс, задаваемый суммами шагов:

$$
S_0=x,
\qquad
S_n=x+\sum_{j=1}^n\xi_j,
$$

где $\xi_j$ - независимые шаги.

### Простое блуждание на $\mathbb Z$

Блуждание с шагами

$$
\xi_j\in\{-1,1\}.
$$

Если

$$
P\{\xi_j=1\}=P\{\xi_j=-1\}=\frac12,
$$

оно называется простым симметричным блужданием.

### Блуждание с одинаково распределенными шагами

Блуждание с одинаково распределенными шагами. В этом случае приращения стационарны.

## 4. Основные теоремы и свойства

- Если

$$
S_n=S_0+\sum_{j=1}^n\xi_j
$$

и $\xi_j$ независимы, то $(S_n)$ имеет независимые приращения.

- Если шаги iid, то приращения независимы и стационарны:

$$
S_{n+k}-S_n\stackrel d=S_k-S_0.
$$

- Распределение суммы независимых шагов - свертка:

$$
P_{S_n}=\mu_1*\cdots*\mu_n.
$$

В iid-случае:

$$
P_{S_n}=\mu^{*n}.
$$

- Характеристические функции независимых шагов перемножаются:

$$
\varphi_{S_n}(t)=\prod_{j=1}^n\varphi_{\xi_j}(t).
$$

В iid-случае:

$$
\varphi_{S_n}(t)=\left(\varphi_\xi(t)\right)^n.
$$

- При существовании моментов:

$$
\mathbb ES_n=S_0+\sum_{j=1}^n\mathbb E\xi_j,
$$

$$
\operatorname{Var}S_n=\sum_{j=1}^n\operatorname{Var}\xi_j.
$$

- Случайное блуждание с независимыми шагами является марковской цепью.
- Блуждание обычно не стационарно по значениям, но при iid-шагах имеет стационарные приращения.
- Для iid-шагов применимы ЗБЧ и ЦПТ:

$$
\frac{S_n}{n}\xrightarrow{P}\mathbb E\xi_1,
\qquad
\frac{S_n-n\mathbb E\xi_1}{\sqrt{n\operatorname{Var}\xi_1}}
\Rightarrow N(0,1).
$$

## 5. Примеры

### Простое симметричное блуждание

Пусть

$$
P\{\xi_k=1\}=P\{\xi_k=-1\}=\frac12.
$$

Тогда

$$
S_n=\xi_1+\cdots+\xi_n
$$

и

$$
P\{S_n=x\}
=
\binom n{(n+x)/2}2^{-n},
$$

если $n+x$ четно и $|x|\le n$. Иначе вероятность равна $0$.

Среднее и дисперсия:

$$
\mathbb ES_n=0,
\qquad
\operatorname{Var}S_n=n.
$$

### Несимметричное блуждание

Пусть

$$
P\{\xi_k=1\}=p,
\qquad
P\{\xi_k=-1\}=q=1-p.
$$

Тогда

$$
\mathbb ES_n=n(p-q),
\qquad
\operatorname{Var}S_n=4npq.
$$

Если $p>q$, блуждание имеет положительный дрейф; если $p<q$, отрицательный.

### Сумма Бернулли

Пусть

$$
\xi_k\sim \operatorname{Bernoulli}(p),
\qquad
\xi_k\in\{0,1\}.
$$

Тогда

$$
S_n=\sum_{k=1}^n\xi_k
$$

считает число успехов за $n$ испытаний:

$$
S_n\sim \operatorname{Bin}(n,p).
$$

Это процесс с независимыми стационарными приращениями, но он не является блужданием на всей $\mathbb Z$, а является неубывающим счетчиком.

### Блуждание в $\mathbb R^d$

Пусть шаги $\xi_k$ принимают значения в $\mathbb R^d$. Тогда

$$
S_n=S_0+\xi_1+\cdots+\xi_n
$$

задает блуждание в многомерном пространстве. Например, на решетке $\mathbb Z^2$ можно на каждом шаге переходить к одному из соседей.

### Пуассоновский процесс как аналог

Пуассоновский процесс $N(t)$ в непрерывном времени имеет независимые и стационарные приращения:

$$
N(t+s)-N(t)\sim \operatorname{Poisson}(\lambda s).
$$

Это не дискретная случайная последовательность, но полезный аналог: он показывает, что идея независимых приращений используется и в непрерывном времени.

## 6. Схемы доказательств / обоснований

### Почему сумма независимых шагов имеет независимые приращения

Пусть

$$
S_n=\sum_{j=1}^n\xi_j.
$$

Для моментов

$$
0\le n_0<n_1<\cdots<n_k
$$

приращение

$$
S_{n_r}-S_{n_{r-1}}
=
\sum_{j=n_{r-1}+1}^{n_r}\xi_j.
$$

Разные приращения зависят от непересекающихся наборов независимых шагов. Поэтому они независимы.

### Почему характеристическая функция суммы факторизуется

Если $\xi_j$ независимы, то

$$
\varphi_{S_n}(t)
=
\mathbb E\exp\left(it\sum_{j=1}^n\xi_j\right)
=
\mathbb E\prod_{j=1}^n e^{it\xi_j}.
$$

По независимости математическое ожидание произведения равно произведению математических ожиданий:

$$
\varphi_{S_n}(t)
=
\prod_{j=1}^n\mathbb E e^{it\xi_j}
=
\prod_{j=1}^n\varphi_{\xi_j}(t).
$$

### Почему блуждание является марковской цепью

Для блуждания

$$
S_{n+1}=S_n+\xi_{n+1}.
$$

Шаг $\xi_{n+1}$ независим от прошлого

$$
S_0,\ldots,S_n.
$$

Поэтому условное распределение $S_{n+1}$ при известном прошлом зависит только от $S_n$. В счетном случае:

$$
P\{S_{n+1}=j\mid S_0=i_0,\ldots,S_n=i\}
=
P\{\xi_{n+1}=j-i\}.
$$

Это марковское свойство.

### Почему блуждание не стационарно по значениям

Для симметричного блуждания

$$
\operatorname{Var}S_n=n.
$$

Значит распределение $S_n$ зависит от $n$. Например, $S_0=0$ почти наверное, а $S_1$ принимает значения $\pm1$. Поэтому значения процесса не стационарны, хотя приращения стационарны.

## 7. Что написать на доске

1. Приращение:

$$
S_n-S_m.
$$

2. Независимые приращения:

$$
S_{n_1}-S_{n_0},\ldots,S_{n_k}-S_{n_{k-1}}
\quad\text{независимы}.
$$

3. Блуждание:

$$
S_0=0,
\qquad
S_n=\sum_{j=1}^n\xi_j.
$$

4. Стационарные приращения при iid-шагах:

$$
S_{n+k}-S_n\stackrel d=S_k.
$$

5. Характеристическая функция:

$$
\varphi_{S_n}(t)=\left(\varphi_\xi(t)\right)^n.
$$

6. Моменты:

$$
\mathbb ES_n=n\mathbb E\xi_1,
\qquad
\operatorname{Var}S_n=n\operatorname{Var}\xi_1.
$$

7. Простое блуждание:

$$
P\{\xi=1\}=p,
\qquad
P\{\xi=-1\}=q.
$$

## 8. Типичные дополнительные вопросы

**Что является главным объектом билета?**

Случайная последовательность, у которой независимы приращения на непересекающихся промежутках. Главный пример - случайное блуждание, то есть сумма независимых шагов.

**В чем разница между независимыми значениями и независимыми приращениями?**

Независимые значения означали бы независимость $S_0,S_1,\ldots$. Независимые приращения означают независимость разностей $S_n-S_m$ на непересекающихся промежутках. У блуждания значения обычно зависимы, но приращения независимы.

**Что значит стационарность приращений?**

Это значит, что закон приращения зависит только от длины промежутка:

$$
S_{n+k}-S_n\stackrel d=S_k-S_0.
$$

При $S_0=0$:

$$
S_{n+k}-S_n\stackrel d=S_k.
$$

**Почему iid-шаги дают независимые стационарные приращения?**

Независимость приращений следует из независимости непересекающихся наборов шагов. Стационарность следует из одинакового распределения шагов: сумма любых $k$ последовательных шагов имеет один и тот же закон.

**Как найти распределение $S_n$?**

В общем случае это свертка законов шагов:

$$
P_{S_n}=\mu_1*\cdots*\mu_n.
$$

В iid-случае:

$$
P_{S_n}=\mu^{*n}.
$$

Для простого блуждания получается биномиальная формула.

**Почему характеристическая функция равна степени?**

Потому что характеристическая функция суммы независимых величин равна произведению характеристических функций:

$$
\varphi_{S_n}(t)
=
\prod_{j=1}^n\varphi_{\xi_j}(t).
$$

Если шаги одинаково распределены:

$$
\varphi_{S_n}(t)=\varphi_\xi(t)^n.
$$

**Почему блуждание является марковской цепью?**

Потому что

$$
S_{n+1}=S_n+\xi_{n+1},
$$

а новый шаг независим от прошлого. Поэтому при известном $S_n$ прошлые состояния не дают дополнительной информации о $S_{n+1}$.

**Случайное блуждание стационарно?**

По значениям обычно нет: распределение $S_n$ меняется с $n$. Но при iid-шагах у него стационарные приращения.

**Как связаны блуждания с ЗБЧ и ЦПТ?**

Если шаги iid, то

$$
\frac{S_n}{n}\xrightarrow{P}\mathbb E\xi_1
$$

по закону больших чисел, а при конечной ненулевой дисперсии

$$
\frac{S_n-n\mathbb E\xi_1}{\sqrt{n\operatorname{Var}\xi_1}}
\Rightarrow N(0,1).
$$

**Какая главная ошибка в этом билете?**

Путать независимость приращений с независимостью самих значений процесса и путать стационарность приращений со стационарностью процесса.

## 9. Типичные ошибки

- Путать независимые значения и независимые приращения.
- Считать, что случайное блуждание стационарно по значениям.
- Забывать начальное состояние $S_0$.
- Забывать, что стационарность приращений требует одинакового распределения шагов.
- Писать $\varphi_{S_n}=\varphi_\xi^n$ без условия одинакового распределения шагов.
- Складывать дисперсии без независимости шагов.
- Путать блуждание на $\mathbb Z$ и счетчик Бернулли: во втором случае шаги $0/1$, а не $\pm1$.
- Считать марковское свойство полной независимостью состояний.

## 10. Связи с другими билетами

- [[09|Билет 09]]: независимость и факторизация математических ожиданий используются для приращений и характеристических функций.
- [[16|Билет 16]]: блуждание - случайная последовательность и случайный элемент пространства последовательностей.
- [[17|Билет 17]]: у блуждания при iid-шагах стационарные приращения, но обычно не стационарные значения.
- [[21|Билет 21]]: ЗБЧ и ЦПТ описывают асимптотику $S_n$.
- [[22|Билет 22]]: блуждание задает переходную матрицу $p_{i,i+1}=p$, $p_{i,i-1}=q$.
- [[23|Билет 23]]: случайное блуждание - пример марковской цепи.

## 11. Минимум на удовлетворительно

Нужно сказать:

1. Приращение процесса:

$$
S_n-S_m.
$$

2. Независимые приращения: приращения на непересекающихся промежутках независимы.

3. Случайное блуждание:

$$
S_0=0,
\qquad
S_n=\xi_1+\cdots+\xi_n,
$$

где $\xi_k$ независимы.

4. Если шаги iid:

$$
\varphi_{S_n}(t)=\varphi_\xi(t)^n.
$$

5. Главная ловушка: независимые приращения не означают независимые значения.

## 12. Минимум на хорошо

Помимо минимума, нужно объяснить:

- что такое стационарные приращения;
- почему сумма независимых шагов имеет независимые приращения;
- как распределение суммы связано со сверткой;
- почему характеристические функции перемножаются;
- как считаются $\mathbb ES_n$ и $\operatorname{Var}S_n$;
- почему случайное блуждание является марковской цепью;
- разобрать простое симметричное блуждание.

## 13. Что добавить для отлично

Для сильного ответа стоит добавить:

- формулу распределения простого блуждания:

$$
P\{S_n=x\}
=
\binom n{(n+x)/2}p^{(n+x)/2}q^{(n-x)/2};
$$

- условие четности $n+x$;
- различие между стационарностью процесса и стационарностью приращений;
- переходные вероятности блуждания как марковской цепи;
- связь с ЗБЧ и ЦПТ;
- пример счетчика Бернулли и пуассоновского процесса как аналога.

## 14. Устная версия ответа на 5 минут

1. Приращение последовательности - это разность $S_n-S_m$.
2. Процесс имеет независимые приращения, если приращения на непересекающихся промежутках независимы.
3. Главный пример - случайное блуждание:

$$
S_0=0,
\qquad
S_n=\xi_1+\cdots+\xi_n.
$$

4. Если шаги $\xi_k$ независимы, то приращения независимы, потому что каждое приращение является суммой своего набора шагов.
5. Если шаги одинаково распределены, то приращения стационарны:

$$
S_{n+k}-S_n\stackrel d=S_k.
$$

6. Распределение $S_n$ - свертка законов шагов.
7. Через характеристические функции:

$$
\varphi_{S_n}(t)=\prod_{j=1}^n\varphi_{\xi_j}(t),
$$

а в iid-случае:

$$
\varphi_{S_n}(t)=\varphi_\xi(t)^n.
$$

8. При существовании моментов:

$$
\mathbb ES_n=n\mathbb E\xi_1,
\qquad
\operatorname{Var}S_n=n\operatorname{Var}\xi_1.
$$

9. Для простого блуждания $\xi_k=\pm1$, вероятности шагов $p$ и $q$.
10. Блуждание является марковской цепью, потому что следующий шаг независим от прошлого.
11. По значениям блуждание обычно не стационарно, но при iid-шагах имеет стационарные приращения.
12. Асимптотика описывается ЗБЧ и ЦПТ.

## 15. Если осталось 2 минуты перед экзаменом

Запомнить:

$$
S_n-S_m
$$

- приращение.

Независимые приращения:

$$
S_{n_1}-S_{n_0},\ldots,S_{n_k}-S_{n_{k-1}}
\quad\text{независимы}.
$$

Блуждание:

$$
S_0=0,
\qquad
S_n=\sum_{j=1}^n\xi_j.
$$

При iid-шагах:

$$
S_{n+k}-S_n\stackrel d=S_k,
\qquad
\varphi_{S_n}(t)=\varphi_\xi(t)^n.
$$

Главные ловушки:

- независимые приращения не означают независимые значения;
- стационарные приращения не означают стационарный процесс;
- формула $\varphi_{S_n}=\varphi_\xi^n$ требует iid-шагов.
