# Билет 17. Стационарные и эргодические случайные последовательности в широком и узком смыслах

Источники: Ширяев 2, гл. V-VI; лекции ДСП.

## 1. Короткий ответ

Случайная последовательность

$$
\{X_n:n\in\mathbb Z\}
$$

называется стационарной в узком смысле, если ее конечномерные распределения не меняются при сдвиге времени:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k})
$$

для всех $k$, $n_1,\ldots,n_k$ и целых $h$.

Стационарность в широком смысле требует только существования вторых моментов, постоянного среднего и ковариации, зависящей только от лага:

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k).
$$

Эргодичность в узком смысле означает, что сдвиг-инвариантные события имеют вероятность только $0$ или $1$. Интуитивно: одна достаточно длинная траектория отражает вероятностные свойства процесса.

Эргодичность в широком смысле обычно понимают как сходимость временного среднего к математическому ожиданию в среднем квадратичном:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\xrightarrow[L^2]{N\to\infty}
m.
$$

## 2. Полный ответ

### 17.1. Введение и основная идея

Этот билет продолжает [[16|Билет 16]], где дискретный процесс рассматривался как случайная последовательность и как случайный элемент пространства последовательностей. Теперь нас интересуют свойства последовательности, которые описывают поведение при сдвиге времени.

Пусть

$$
X=(X_n)_{n\in\mathbb Z}
$$

или

$$
X=(X_n)_{n\ge0}
$$

- случайная последовательность. Для стационарности удобнее работать с двусторонним временем $n\in\mathbb Z$, потому что сдвиг можно применять в обе стороны. Для экзамена можно формулировать и для $n\ge0$, но тогда сдвиги берут только такие, чтобы индексы оставались неотрицательными.

Главная идея стационарности: статистические свойства процесса не меняются при переносе начала отсчета времени. Если сегодня процесс выглядит так же, как завтра, в смысле распределений, то он стационарен.

### 17.2. Стационарность в узком смысле

Сначала определим стационарность в узком смысле. Последовательность называется стационарной в узком смысле, или строго стационарной, если для любых

$$
k\ge1,\qquad n_1,\ldots,n_k\in\mathbb Z,\qquad h\in\mathbb Z
$$

выполнено равенство распределений:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

Это означает, что все конечномерные распределения инвариантны относительно сдвига времени. Например,

$$
X_0\stackrel d=X_{10},
$$

но этого мало: также должно быть

$$
(X_0,X_1,X_5)\stackrel d=(X_{10},X_{11},X_{15})
$$

и аналогично для любого конечного набора моментов.

На языке пространства последовательностей из [[16|Билета 16]] стационарность в узком смысле можно выразить через сдвиг. Пусть

$$
S:E^{\mathbb Z}\to E^{\mathbb Z}
$$

задан формулой

$$
(Sx)_n=x_{n+1}.
$$

Если $P_X$ - закон последовательности на $E^{\mathbb Z}$, то узкая стационарность означает

$$
P_X\circ S^{-1}=P_X.
$$

То есть закон процесса как мера на пространстве траекторий инвариантен относительно сдвига.

### 17.3. Стационарность в широком смысле

Теперь стационарность в широком смысле. Она относится только к характеристикам второго порядка, поэтому требует

$$
\mathbb E X_n^2<\infty
$$

для всех $n$. Последовательность стационарна в широком смысле, если:

1. среднее не зависит от времени:

$$
\mathbb E X_n=m;
$$

2. ковариация зависит только от разности индексов:

$$
\operatorname{Cov}(X_n,X_m)=R(m-n).
$$

Часто записывают

$$
R(k)=\operatorname{Cov}(X_n,X_{n+k}).
$$

Если последовательность центрирована, то есть $m=0$, то

$$
R(k)=\mathbb E[X_nX_{n+k}].
$$

Иногда в курсах случайных процессов корреляционной функцией называют именно

$$
\mathbb E[X_nX_{n+k}],
$$

а ковариационной - центрированную величину. Важно проговорить, какая версия используется. В этих билетах удобнее держать в голове:

$$
\operatorname{Cov}(X_n,X_{n+k})
=
\mathbb E[(X_n-m)(X_{n+k}-m)].
$$

### 17.4. Связь между типами стационарности

Узкая стационарность при существовании вторых моментов влечет широкую стационарность. Действительно, из равенства одномерных распределений следует одинаковое среднее:

$$
\mathbb E X_n=m.
$$

Из равенства двумерных распределений

$$
(X_n,X_{n+k})\stackrel d=(X_0,X_k)
$$

следует

$$
\operatorname{Cov}(X_n,X_{n+k})
=
\operatorname{Cov}(X_0,X_k).
$$

Обратное в общем случае неверно: широкая стационарность контролирует только первые два момента, но не все распределение.

Есть важное исключение: для гауссовских последовательностей широкая стационарность влечет узкую. Причина в том, что конечномерные распределения гауссовского процесса полностью задаются средним вектором и ковариационной матрицей. Если среднее постоянно, а ковариация зависит только от лага, то все конечномерные гауссовские распределения инвариантны при сдвиге. Это связь с [[18|Билетом 18]].

### 17.5. Понятие эргодичности и инвариантные события

Теперь эргодичность. Стационарность говорит, что закон не меняется при сдвиге. Эргодичность говорит сильнее: в стационарном процессе нет скрытой случайной характеристики, которая сохраняется при сдвиге и разбивает траектории на разные режимы.

На пространстве траекторий событие $A$ называется сдвиг-инвариантным, если

$$
S^{-1}A=A
$$

с точностью до множеств вероятности ноль. Интуитивно такое событие не меняется, если сдвинуть всю последовательность на один шаг.

Стационарная последовательность называется эргодической в узком смысле, если любое сдвиг-инвариантное событие имеет вероятность $0$ или $1$:

$$
A=S^{-1}A
\quad\Longrightarrow\quad
P_X(A)\in\{0,1\}.
$$

Это определение часто называют метрической эргодичностью. Его смысл: сдвиг не оставляет нетривиальных случайных "меток" траектории.

### 17.6. Практическая интерпретация и временные средние

Практическая интерпретация эргодичности связана с временными средними. Если процесс стационарен и эргодичен в узком смысле, то по эргодической теореме для интегрируемой функции $f$:

$$
\frac1N\sum_{n=0}^{N-1} f(X_n)
\to
\mathbb E f(X_0)
$$

почти наверное, при подходящих стандартных условиях. В частности, для $f(x)=x$, если $\mathbb E|X_0|<\infty$:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\to
\mathbb E X_0
$$

почти наверное.

Это и есть главный прикладной смысл: среднее по одной длинной реализации можно использовать вместо среднего по ансамблю.

### 17.7. Эргодичность в широком смысле

Эргодичность в широком смысле - более слабое понятие второго порядка. Для стационарной в широком смысле последовательности с конечными вторыми моментами говорят, что она эргодична по среднему, или эргодична в широком смысле относительно среднего, если

$$
\overline X_N
=
\frac1N\sum_{n=0}^{N-1}X_n
$$

сходится к $m=\mathbb EX_0$ в среднем квадратичном:

$$
\mathbb E|\overline X_N-m|^2\to0.
$$

Эквивалентно,

$$
\operatorname{Var}(\overline X_N)\to0.
$$

Для стационарной в широком смысле последовательности

$$
\operatorname{Var}(\overline X_N)
=
\frac1{N^2}
\sum_{i=0}^{N-1}\sum_{j=0}^{N-1}R(j-i),
$$

где

$$
R(k)=\operatorname{Cov}(X_0,X_k).
$$

Поэтому условия затухания корреляций дают эргодичность среднего. Это подробно развивается в [[24|Билете 24]].

### 17.8. Сравнение понятий и примеры

Важно различать:

1. стационарность - инвариантность распределений или моментов при сдвиге;
2. эргодичность - возможность восстанавливать вероятностные характеристики по одной длинной реализации;
3. независимость - совсем другое свойство, гораздо сильнее во многих примерах.

Например, iid-последовательность стационарна и эргодична, но стационарность сама по себе не означает независимость. Последовательность $X_n=Y$, где $Y$ - одна случайная величина, стационарна, но не эргодична: траектория хранит неизменный случайный уровень $Y$.

## 3. Основные определения

### Сдвиг на пространстве последовательностей

Для двусторонних последовательностей $x=(x_n)_{n\in\mathbb Z}$:

$$
(Sx)_n=x_{n+1}.
$$

### Стационарность в узком смысле

Последовательность $(X_n)$ стационарна в узком смысле, если для любых $k$, $n_1,\ldots,n_k$ и $h$:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

### Стационарность в широком смысле

Последовательность с конечными вторыми моментами стационарна в широком смысле, если

$$
\mathbb E X_n=m
$$

не зависит от $n$, а

$$
\operatorname{Cov}(X_n,X_m)=R(m-n)
$$

зависит только от лага.

### Корреляционная или ковариационная функция стационарной последовательности

Для центрированной последовательности:

$$
R(k)=\mathbb E[X_0X_k].
$$

В общем случае:

$$
R(k)=\operatorname{Cov}(X_0,X_k).
$$

### Сдвиг-инвариантное событие

Событие $A$ на пространстве траекторий такое, что

$$
S^{-1}A=A
$$

с точностью до множеств вероятности ноль.

### Эргодичность в узком смысле

Стационарная последовательность эргодична, если каждое сдвиг-инвариантное событие имеет вероятность $0$ или $1$:

$$
S^{-1}A=A
\quad\Rightarrow\quad
P(A)\in\{0,1\}.
$$

### Эргодичность в широком смысле по среднему

Стационарная в широком смысле последовательность эргодична по среднему, если

$$
\frac1N\sum_{n=0}^{N-1}X_n
\xrightarrow[L^2]{}
\mathbb E X_0.
$$

## 4. Основные теоремы и свойства

- Узкая стационарность означает инвариантность закона процесса относительно сдвига:

$$
P_X\circ S^{-1}=P_X.
$$

- Узкая стационарность и конечные вторые моменты влекут широкую стационарность.
- Широкая стационарность в общем случае не влечет узкую.
- Для гауссовской последовательности широкая стационарность влечет узкую.
- Эргодичность в узком смысле равносильна отсутствию нетривиальных сдвиг-инвариантных событий.
- Для стационарной эргодической последовательности временные средние интегрируемых функций совпадают с математическими ожиданиями:

$$
\frac1N\sum_{n=0}^{N-1}f(X_n)\to\mathbb E f(X_0)
$$

почти наверное.

- Для стационарной в широком смысле последовательности эргодичность среднего связана с условием:

$$
\operatorname{Var}\left(\frac1N\sum_{n=0}^{N-1}X_n\right)\to0.
$$

- Если ковариации достаточно быстро затухают, то временное среднее сходится к математическому ожиданию в $L^2$.

## 5. Примеры

### iid-последовательность

Пусть

$$
X_0,X_1,\ldots
$$

независимы и одинаково распределены.

Тогда последовательность стационарна в узком смысле: любой конечный вектор имеет произведение одних и тех же одномерных законов, и сдвиг времени не меняет это произведение.

Если $\mathbb E X_0^2<\infty$, то она стационарна и в широком смысле:

$$
\mathbb E X_n=m,
$$

и

$$
\operatorname{Cov}(X_n,X_{n+k})=
\begin{cases}
\operatorname{Var}X_0, & k=0,\\
0, & k\ne0.
\end{cases}
$$

Такая последовательность эргодична: временные средние сходятся к математическому ожиданию по закону больших чисел.

### Случайная константа

Пусть

$$
X_n=Y
$$

для всех $n$, где $Y$ - неконстантная случайная величина.

Эта последовательность стационарна в узком смысле: сдвиг времени вообще ничего не меняет. Если $\mathbb E Y^2<\infty$, она стационарна и в широком смысле:

$$
R(k)=\operatorname{Var}Y
$$

для всех $k$.

Но она не эргодична. Временное среднее равно

$$
\frac1N\sum_{n=0}^{N-1}X_n=Y,
$$

а не константе $\mathbb EY$. Одна траектория показывает только свой случайный уровень.

### Широкая стационарность без узкой

Пусть величины $X_n$ независимы, имеют нулевое среднее и единичную дисперсию, но распределения чередуются:

$$
X_{2k}\sim N(0,1),
\qquad
X_{2k+1}\sim
\begin{cases}
1, & 1/2,\\
-1, & 1/2.
\end{cases}
$$

Тогда

$$
\mathbb E X_n=0,
\qquad
\operatorname{Var}X_n=1,
$$

а при $n\ne m$:

$$
\operatorname{Cov}(X_n,X_m)=0
$$

из-за независимости. Значит последовательность стационарна в широком смысле:

$$
R(0)=1,\qquad R(k)=0,\ k\ne0.
$$

Но она не стационарна в узком смысле, потому что одномерное распределение зависит от четности $n$.

### Гауссовская стационарная последовательность

Если последовательность гауссовская, то ее конечномерные распределения полностью задаются средними и ковариациями. Поэтому для гауссовской последовательности условия

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k)
$$

уже дают узкую стационарность.

### Гармоника со случайной фазой

Пусть

$$
X_n=\cos(\lambda n+\Phi),
$$

где $\Phi$ равномерна на $[0,2\pi]$. Тогда сдвиг $n\mapsto n+h$ заменяет фазу $\Phi$ на $\Phi+\lambda h$, а равномерное распределение фазы не меняется. Поэтому последовательность стационарна в узком смысле.

Этот пример полезен тем, что зависимость во времени сильная, но распределения при сдвиге не меняются.

## 6. Схемы доказательств / обоснований

### Почему узкая стационарность влечет широкую

Пусть последовательность стационарна в узком смысле и

$$
\mathbb E X_n^2<\infty.
$$

Из

$$
X_n\stackrel d=X_0
$$

следует

$$
\mathbb E X_n=\mathbb E X_0=m.
$$

Из

$$
(X_n,X_{n+k})\stackrel d=(X_0,X_k)
$$

следует равенство вторых смешанных моментов:

$$
\mathbb E[X_nX_{n+k}]=\mathbb E[X_0X_k].
$$

Значит

$$
\operatorname{Cov}(X_n,X_{n+k})
=
\operatorname{Cov}(X_0,X_k),
$$

то есть ковариация зависит только от лага.

### Почему широкая стационарность не влечет узкую

Широкая стационарность фиксирует только

$$
\mathbb E X_n
$$

и

$$
\operatorname{Cov}(X_n,X_m).
$$

Она не видит третьи, четвертые и вообще все более высокие распределительные характеристики. Поэтому можно сделать последовательность с одинаковыми средними и ковариациями, но с разными одномерными распределениями в разные моменты времени. Пример с чередованием нормального и радемахеровского распределения показывает это явно.

### Почему для гауссовских широкая стационарность дает узкую

Гауссовский вектор полностью определяется средним вектором и ковариационной матрицей. Для любого набора моментов

$$
n_1,\ldots,n_k
$$

средний вектор равен

$$
(m,\ldots,m),
$$

а ковариационная матрица имеет элементы

$$
\operatorname{Cov}(X_{n_i},X_{n_j})
=
R(n_j-n_i).
$$

После сдвига всех индексов на $h$ разности не меняются:

$$
(n_j+h)-(n_i+h)=n_j-n_i.
$$

Значит средний вектор и ковариационная матрица не меняются, следовательно не меняется и гауссовское конечномерное распределение.

### Почему случайная константа не эргодична

Если $X_n=Y$, то событие

$$
A=\{Y>0\}
$$

не меняется при сдвиге траектории. Если

$$
0<P\{Y>0\}<1,
$$

то это нетривиальное сдвиг-инвариантное событие. Значит процесс не эргодичен.

## 7. Что написать на доске

1. Узкая стационарность:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

2. Сдвиг:

$$
(Sx)_n=x_{n+1},
\qquad
P_X\circ S^{-1}=P_X.
$$

3. Широкая стационарность:

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k).
$$

4. Узкая плюс вторые моменты:

$$
\text{узкая стационарность}+\mathbb E X_n^2<\infty
\Longrightarrow
\text{широкая стационарность}.
$$

5. Эргодичность:

$$
S^{-1}A=A
\Longrightarrow
P(A)\in\{0,1\}.
$$

6. Эргодичность среднего:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\to
\mathbb E X_0.
$$

Для широкого смысла обычно:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\xrightarrow{L^2}
\mathbb E X_0.
$$

## 8. Типичные дополнительные вопросы

**Что является главным объектом билета?**

Случайная последовательность $(X_n)$ и ее поведение при сдвиге времени. Нужно различать инвариантность всех конечномерных распределений, инвариантность первых двух моментов и эргодичность.

**В чем разница между узкой и широкой стационарностью?**

Узкая стационарность требует равенства всех конечномерных распределений после сдвига:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

Широкая стационарность требует только постоянного среднего и ковариации, зависящей от лага:

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k).
$$

**Почему узкая стационарность сильнее широкой?**

Потому что она фиксирует все конечномерные распределения. Если есть вторые моменты, из этих распределений следуют и средние, и ковариации. Широкая стационарность фиксирует только первые два момента.

**Когда широкая стационарность влечет узкую?**

Для гауссовских последовательностей. Гауссовские конечномерные распределения полностью задаются средним вектором и ковариационной матрицей.

**Что такое эргодичность в узком смысле?**

Это отсутствие нетривиальных сдвиг-инвариантных событий:

$$
S^{-1}A=A
\Rightarrow
P(A)\in\{0,1\}.
$$

Интуитивно одна длинная траектория достаточно хорошо представляет весь ансамбль.

**Что такое эргодичность в широком смысле?**

Обычно в этом курсе это эргодичность среднего: временное среднее сходится к математическому ожиданию в $L^2$:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\xrightarrow{L^2}
\mathbb E X_0.
$$

**Стационарность означает независимость?**

Нет. Стационарность говорит об инвариантности распределений при сдвиге, но не запрещает зависимость во времени. Например, $X_n=Y$ для всех $n$ стационарна, но координаты полностью зависимы.

**Эргодичность означает независимость?**

Нет. Независимые одинаково распределенные последовательности обычно эргодичны, но эргодичность не равна независимости. Эргодичность запрещает сдвиг-инвариантные случайные режимы, но допускает зависимость.

**Почему случайная константа стационарна, но не эргодична?**

Потому что сдвиг не меняет траекторию, значит стационарность есть. Но временное среднее равно случайной величине $Y$, а не константе $\mathbb EY$. Кроме того, событие $\{Y>0\}$ сдвиг-инвариантно и может иметь вероятность между $0$ и $1$.

**Как билет связан с [[24|Билетом 24]]?**

В [[24|Билете 24]] изучается корреляционная функция

$$
R(k)=\operatorname{Cov}(X_0,X_k)
$$

и достаточные условия, при которых временное среднее сходится к математическому ожиданию в $L^2$.

## 9. Типичные ошибки

- Смешивать стационарность и эргодичность.
- Считать, что стационарность означает независимость.
- Считать, что широкая стационарность всегда равносильна узкой.
- Забывать условие существования вторых моментов для широкой стационарности.
- Проверять только одномерные распределения и объявлять узкую стационарность.
- Называть процесс эргодическим без указания смысла: узкий смысл, среднее, $L^2$.
- Путать ковариационную функцию $R(k)$ и коэффициент корреляции.
- Забывать, что случайная константа является стационарной, но не эргодической.

## 10. Связи с другими билетами

- [[16|Билет 16]]: стационарность формулируется для закона на пространстве последовательностей.
- [[15|Билет 15]]: узкая стационарность - свойство согласованных конечномерных распределений.
- [[14|Билет 14]]: широкая стационарность использует ковариации и геометрию второго порядка.
- [[18|Билет 18]]: для гауссовских последовательностей широкая стационарность влечет узкую.
- [[21|Билет 21]]: временные средние и закон больших чисел связаны с эргодической идеей.
- [[24|Билет 24]]: корреляционная функция и достаточные условия эргодичности среднего.
- [[25|Билет 25]]: спектральные характеристики строятся для стационарных последовательностей второго порядка.

## 11. Минимум на удовлетворительно

Нужно сказать:

1. Узкая стационарность:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

2. Широкая стационарность:

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k).
$$

3. Узкая плюс вторые моменты влечет широкую.

4. Эргодичность: временные средние отражают вероятностные средние; строго - нет нетривиальных сдвиг-инвариантных событий.

5. Главная ловушка: стационарность не равна эргодичности и не равна независимости.

## 12. Минимум на хорошо

Помимо минимума, нужно объяснить:

- что такое сдвиг на пространстве последовательностей;
- почему узкая стационарность означает инвариантность закона относительно сдвига;
- почему широкая стационарность требует вторых моментов;
- почему широкая не всегда влечет узкую;
- почему для гауссовских последовательностей широкая стационарность влечет узкую;
- разобрать пример iid-последовательности и случайной константы.

## 13. Что добавить для отлично

Для сильного ответа стоит добавить:

- формулировку эргодичности через сдвиг-инвариантные события;
- интерпретацию через эргодическую теорему:

$$
\frac1N\sum_{n=0}^{N-1}f(X_n)\to\mathbb E f(X_0);
$$

- определение эргодичности в широком смысле как $L^2$-сходимости среднего;
- формулу для дисперсии среднего:

$$
\operatorname{Var}(\overline X_N)
=
\frac1{N^2}\sum_{i,j=0}^{N-1}R(j-i);
$$

- пример широкой стационарности без узкой;
- пример стационарной неэргодической последовательности;
- связь со спектральной теорией и корреляционной функцией.

## 14. Устная версия ответа на 5 минут

1. В этом билете изучаются свойства случайной последовательности при сдвиге времени.
2. Узкая стационарность означает, что все конечномерные распределения не меняются при сдвиге:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

3. На пространстве траекторий это означает инвариантность закона относительно сдвига:

$$
P_X\circ S^{-1}=P_X.
$$

4. Широкая стационарность требует вторых моментов, постоянного среднего и ковариации только от лага:

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k).
$$

5. Узкая стационарность при наличии вторых моментов влечет широкую.
6. Обратное неверно, потому что широкая стационарность контролирует только первые два момента.
7. Для гауссовских последовательностей обратное верно: среднее и ковариация задают все конечномерные распределения.
8. Эргодичность в узком смысле означает, что сдвиг-инвариантные события имеют вероятность $0$ или $1$.
9. Практически это означает, что временные средние совпадают с вероятностными:

$$
\frac1N\sum_{n=0}^{N-1}f(X_n)\to\mathbb E f(X_0).
$$

10. В широком смысле обычно говорят об эргодичности среднего:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\xrightarrow{L^2}
\mathbb E X_0.
$$

11. Примеры: iid-последовательность стационарна и эргодична; случайная константа стационарна, но не эргодична.
12. Завершить можно связью с корреляционной функцией: условия на $R(k)$ дают эргодичность среднего.

## 15. Если осталось 2 минуты перед экзаменом

Запомнить три различия.

Узкая стационарность:

$$
(X_{n_1+h},\ldots,X_{n_k+h})
\stackrel d=
(X_{n_1},\ldots,X_{n_k}).
$$

Широкая стационарность:

$$
\mathbb E X_n=m,
\qquad
\operatorname{Cov}(X_n,X_{n+k})=R(k).
$$

Эргодичность:

$$
S^{-1}A=A
\Rightarrow
P(A)\in\{0,1\},
$$

или для среднего во втором порядке:

$$
\frac1N\sum_{n=0}^{N-1}X_n
\xrightarrow{L^2}
\mathbb E X_0.
$$

Главная ловушка: стационарность, эргодичность и независимость - разные свойства.
