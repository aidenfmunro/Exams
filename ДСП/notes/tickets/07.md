# Билет 07. Простые функции, интеграл Лебега и теоремы Леви и Лебега о предельном переходе

Источники: Ширяев 1, гл. II, §6; лекции ДСП.

## 1. Короткий ответ

Интеграл Лебега по неотрицательной мере строится в три шага: сначала для индикаторов, затем для простых неотрицательных функций, затем для произвольных неотрицательных измеримых функций через супремум по простым минорантам.

Если

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
\qquad
a_k\ge0,\quad A_k\in\mathcal F,
$$

и множества $A_k$ попарно не пересекаются, то

$$
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

Для неотрицательной измеримой функции $f$:

$$
\int f\,d\mu
=
\sup\left\{\int s\,d\mu:\ 0\le s\le f,\ s\text{ простая}\right\}.
$$

Теорема Беппо Леви: если $0\le f_n\uparrow f$, то

$$
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

Мажорантная теорема Лебега: если $f_n\to f$ почти всюду и $|f_n|\le g$, где $g\in L^1$, то

$$
\int f_n\,d\mu\to\int f\,d\mu.
$$

## 2. Полный ответ

### 2.1. Введение и роль интеграла Лебега

В предыдущих билетах были введены измеримые функции и распределения. Теперь нужен аппарат, который позволяет интегрировать измеримые функции по мере. В вероятности этот интеграл становится математическим ожиданием, а в дальнейшем через него выражаются моменты, характеристические функции, условные ожидания, корреляции и спектральные характеристики.

### 2.2. Измеримое пространство и интеграл от индикаторной функции

Пусть задано пространство с неотрицательной мерой

$$
(\Omega,\mathcal F,\mu).
$$

Неотрицательность меры означает, что

$$
\mu(A)\in[0,\infty]
\qquad
A\in\mathcal F.
$$

Сначала интеграл определяют для самой простой измеримой функции - индикатора множества:

$$
\int \mathbf 1_A\,d\mu=\mu(A).
$$

Это естественно: индикатор равен $1$ на $A$ и $0$ вне $A$, значит площадь под ним должна равняться мере множества $A$.

### 2.3. Интеграл от простых функций

Дальше рассматривают простые функции. Простая функция - измеримая функция, принимающая конечное число значений. Если $s\ge0$ и

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
$$

где $a_k\ge0$, $A_k\in\mathcal F$ и $A_k$ попарно не пересекаются, то интеграл задают формулой

$$
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

Эта формула не зависит от выбранного представления простой функции: если разложение измельчить до общего конечного разбиения, сумма останется той же. Поэтому интеграл простой функции определен корректно.

### 2.4. Построение интеграла для неотрицательных измеримых функций

Для произвольной неотрицательной измеримой функции $f$ интеграл строят приближением снизу простыми функциями:

$$
\int f\,d\mu
=
\sup\left\{\int s\,d\mu:\ 0\le s\le f,\ s\text{ простая измеримая}\right\}.
$$

Интуиция такая: сложную функцию можно снизу аппроксимировать ступенчатыми измеримыми функциями. Интеграл $f$ - это наибольший предел площадей таких нижних ступенчатых приближений. Значение интеграла может быть бесконечным.

### 2.5. Функции произвольного знака и условие интегрируемости

Если функция $f$ может принимать отрицательные значения, ее раскладывают на положительную и отрицательную части:

$$
f^+=\max(f,0),\qquad
f^-=\max(-f,0),
$$

так что

$$
f=f^+-f^-,
\qquad
|f|=f^++f^-.
$$

Если хотя бы один из интегралов $\int f^+\,d\mu$, $\int f^-\,d\mu$ конечен, можно определить расширенный интеграл

$$
\int f\,d\mu=\int f^+\,d\mu-\int f^-\,d\mu,
$$

не допуская неопределенности $\infty-\infty$. Если

$$
\int |f|\,d\mu<\infty,
$$

то $f$ называется интегрируемой, или суммируемой, и пишут $f\in L^1(\mu)$.

### 2.6. Теорема Беппо Леви о монотонной сходимости

Главная сила интеграла Лебега - хорошие теоремы о предельном переходе. В программе эти теоремы идут без доказательства, но условия нужно знать точно.

Первая - теорема Беппо Леви, или теорема о монотонной сходимости. Если

$$
0\le f_1\le f_2\le\cdots
$$

и

$$
f_n(\omega)\uparrow f(\omega)
$$

для всех $\omega$ или почти всюду, то

$$
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

Здесь не нужен интегрируемый мажорант; зато нужна неотрицательность и монотонный рост.

### 2.7. Мажорантная теорема Лебега о доминированной сходимости

Вторая - мажорантная теорема Лебега, или теорема Лебега о доминированной сходимости. Если

$$
f_n\to f
$$

почти всюду и существует интегрируемая функция $g\in L^1(\mu)$ такая, что

$$
|f_n|\le g
\qquad \text{почти всюду для всех }n,
$$

то $f$ интегрируема и

$$
\int f_n\,d\mu\to\int f\,d\mu.
$$

Эта теорема позволяет менять предел и интеграл при наличии общего интегрируемого контроля. Без такого контроля утверждение может быть неверным.

## 3. Основные определения

### Пространство с неотрицательной мерой

Тройка $(\Omega,\mathcal F,\mu)$, где $\mathcal F$ - $\sigma$-алгебра, а $\mu:\mathcal F\to[0,\infty]$ - счетно-аддитивная неотрицательная мера.

### Индикатор множества

Для $A\in\mathcal F$:

$$
\mathbf 1_A(\omega)=
\begin{cases}
1, & \omega\in A,\\
0, & \omega\notin A.
\end{cases}
$$

### Простая функция

Измеримая функция $s:\Omega\to\mathbb R$ называется простой, если она принимает конечное число значений. Ее можно записать в виде

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
$$

где $A_k\in\mathcal F$. В каноническом представлении значения $a_k$ различны, а множества

$$
A_k=\{\omega:s(\omega)=a_k\}
$$

попарно не пересекаются и образуют разбиение области, где $s$ принимает эти значения.

### Неотрицательная простая функция

Простая функция $s$, для которой $s(\omega)\ge0$ для всех $\omega$ или почти всюду.

### Интеграл индикатора

$$
\int \mathbf 1_A\,d\mu=\mu(A).
$$

### Интеграл неотрицательной простой функции

Если

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
\qquad
a_k\ge0,
$$

и $A_k$ попарно не пересекаются, то

$$
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

### Интеграл неотрицательной измеримой функции

Для $f:\Omega\to[0,\infty]$:

$$
\int f\,d\mu
=
\sup\left\{\int s\,d\mu:\ 0\le s\le f,\ s\text{ простая измеримая}\right\}.
$$

### Положительная и отрицательная части функции

$$
f^+=\max(f,0),\qquad
f^-=\max(-f,0).
$$

Тогда

$$
f=f^+-f^-,
\qquad
|f|=f^++f^-.
$$

### Интегрируемая функция

Измеримая функция $f$ называется интегрируемой, если

$$
\int |f|\,d\mu<\infty.
$$

В этом случае

$$
\int f\,d\mu=\int f^+\,d\mu-\int f^-\,d\mu
$$

и оба интеграла справа конечны.

### Почти всюду

Свойство выполнено почти всюду, если оно нарушается только на множестве меры ноль.

### Мажоранта

Функция $g$ называется интегрируемой мажорантой для последовательности $f_n$, если

$$
|f_n|\le g\quad \text{почти всюду для всех }n,
\qquad
g\in L^1(\mu).
$$

## 4. Основные теоремы и свойства

### Свойство 1. Корректность интеграла простой функции

Если неотрицательная простая функция имеет два представления

$$
s=\sum_{i=1}^m a_i\mathbf 1_{A_i}
=
\sum_{j=1}^n b_j\mathbf 1_{B_j},
$$

где множества в каждом представлении попарно не пересекаются, то

$$
\sum_{i=1}^m a_i\mu(A_i)
=
\sum_{j=1}^n b_j\mu(B_j).
$$

### Доказательство-идея

Перейдем к общему измельчению

$$
C_{ij}=A_i\cap B_j.
$$

На каждом непустом $C_{ij}$ значения двух представлений совпадают, то есть $a_i=b_j$. Поэтому обе суммы после разбиения на $C_{ij}$ становятся одной и той же суммой.

### Свойство 2. Интеграл индикатора равен мере

Для любого $A\in\mathcal F$:

$$
\int \mathbf 1_A\,d\mu=\mu(A).
$$

Это базовая нормировка интеграла Лебега.

### Свойство 3. Монотонность интеграла

Если $0\le f\le g$, то

$$
\int f\,d\mu\le\int g\,d\mu.
$$

### Доказательство

Любая простая функция $s$, удовлетворяющая $0\le s\le f$, автоматически удовлетворяет $0\le s\le g$. Поэтому супремум по простым минорантам для $f$ не больше супремума для $g$.

### Свойство 4. Линейность для неотрицательных функций

Если $f,g\ge0$ измеримы и $a,b\ge0$, то

$$
\int(af+bg)\,d\mu
=
a\int f\,d\mu+b\int g\,d\mu.
$$

Здесь допускаются бесконечные значения, если не возникает противоречивых операций.

### Доказательство-идея

Для простых функций это следует из формулы суммы по разбиению. Для общих неотрицательных функций берут возрастающие последовательности простых функций, приближающие $f$ и $g$ снизу, и применяют теорему Беппо Леви.

### Свойство 5. Линейность на $L^1$

Если $f,g\in L^1(\mu)$ и $a,b\in\mathbb R$, то

$$
\int(af+bg)\,d\mu
=
a\int f\,d\mu+b\int g\,d\mu.
$$

### Обоснование

Интегрируемость гарантирует конечность интегралов положительных и отрицательных частей, поэтому не возникает неопределенности $\infty-\infty$.

### Свойство 6. Если функция равна нулю почти всюду, то ее интеграл равен нулю

Если $f\ge0$ и $f=0$ почти всюду, то

$$
\int f\,d\mu=0.
$$

Если $f=g$ почти всюду и обе функции интегрируемы, то

$$
\int f\,d\mu=\int g\,d\mu.
$$

### Свойство 7. Аппроксимация неотрицательной измеримой функции простыми

Для любой неотрицательной измеримой функции $f$ существует последовательность неотрицательных простых функций $s_n$ такая, что

$$
0\le s_1\le s_2\le\cdots\le f,
\qquad
s_n(\omega)\uparrow f(\omega).
$$

### Доказательство-идея

Можно взять ступенчатое округление вниз. Например, на уровне $n$ разбить значения $f$ на интервалы длины $2^{-n}$ до высоты $n$ и положить

$$
s_n(\omega)=\frac{k}{2^n}
$$

на множестве

$$
\left\{\frac{k}{2^n}\le f(\omega)<\frac{k+1}{2^n}\right\},
$$

а для $f(\omega)\ge n$ ограничить значение сверху числом $n$. Эти множества измеримы, потому что $f$ измерима.

### Теорема 8. Беппо Леви, или монотонная сходимость

Пусть $f_n:\Omega\to[0,\infty]$ измеримы,

$$
0\le f_1\le f_2\le\cdots,
$$

и

$$
f_n(\omega)\uparrow f(\omega)
$$

почти всюду. Тогда

$$
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

В программе эта теорема идет без доказательства.

### Идея доказательства

Неравенство

$$
\lim_n\int f_n\,d\mu\le\int f\,d\mu
$$

следует из монотонности. Обратное неравенство получают так: берут простую функцию $s\le f$, немного уменьшают ее до $\alpha s$, где $0<\alpha<1$, и используют множества, на которых $f_n$ уже превысила $\alpha s$. Потом переходят к пределу по $n$ и по $\alpha\uparrow1$.

### Теорема 9. Мажорантная теорема Лебега, или доминированная сходимость

Пусть $f_n$ измеримы,

$$
f_n\to f
$$

почти всюду, и существует $g\in L^1(\mu)$ такая, что

$$
|f_n|\le g
\qquad \text{почти всюду для всех }n.
$$

Тогда $f\in L^1(\mu)$ и

$$
\int f_n\,d\mu\to\int f\,d\mu.
$$

Эквивалентно:

$$
\int |f_n-f|\,d\mu\to0.
$$

В программе эта теорема также идет без полного доказательства.

### Идея доказательства

Из $|f_n|\le g$ и $f_n\to f$ следует $|f|\le g$, значит $f$ интегрируема. Далее к неотрицательным функциям

$$
2g-|f_n-f|
$$

или к верхним и нижним оценкам применяют лемму Фату. Смысл: интегрируемая мажоранта запрещает массе убегать в бесконечность или концентрироваться на все меньших множествах с большой высотой.

### Свойство 10. Нужны именно условия предельных теорем

Если $f_n\to f$ почти всюду, но нет ни монотонности, ни интегрируемой мажоранты, то из этого не следует

$$
\int f_n\,d\mu\to\int f\,d\mu.
$$

Например, на $[0,1]$ с мерой Лебега функции

$$
f_n(x)=n\mathbf 1_{(0,1/n)}(x)
$$

сходятся к $0$ почти всюду, но

$$
\int_0^1 f_n(x)\,dx=1
$$

для всех $n$.

## 5. Примеры

### Пример 1. Индикатор

Если $A\in\mathcal F$, то

$$
\int \mathbf 1_A\,d\mu=\mu(A).
$$

В вероятностном пространстве $(\Omega,\mathcal F,P)$:

$$
\mathbb E\mathbf 1_A=P(A).
$$

### Пример 2. Простая функция

Пусть $A,B\in\mathcal F$, $A\cap B=\varnothing$, и

$$
s=2\mathbf 1_A+5\mathbf 1_B.
$$

Тогда

$$
\int s\,d\mu=2\mu(A)+5\mu(B).
$$

Если $\mu(A)=3$, $\mu(B)=1$, то

$$
\int s\,d\mu=11.
$$

### Пример 3. Дискретное математическое ожидание

Пусть $X$ принимает значения $x_1,\ldots,x_n$ с вероятностями $p_1,\ldots,p_n$. Тогда

$$
X=\sum_{k=1}^n x_k\mathbf 1_{\{X=x_k\}},
$$

и

$$
\mathbb E X=\int X\,dP=\sum_{k=1}^n x_kp_k
$$

при условии, что $X$ неотрицательна или интегрируема. Это обычная формула ожидания для дискретной величины.

### Пример 4. Интеграл по плотности

Если $X$ имеет плотность $p$ на $\mathbb R$, то вероятность события $X\in B$ выражается как интеграл индикатора по распределению:

$$
P\{X\in B\}=\int \mathbf 1_B(x)p(x)\,dx.
$$

Это связывает билет с распределениями из [[06|Билета 06]].

### Пример 5. Теорема Беппо Леви

На $[0,1]$ пусть

$$
f_n(x)=\mathbf 1_{[0,1-1/n]}(x).
$$

Тогда

$$
0\le f_n\uparrow \mathbf 1_{[0,1)}
$$

и

$$
\int_0^1 f_n(x)\,dx=1-\frac1n
\uparrow
1
=
\int_0^1 \mathbf 1_{[0,1)}(x)\,dx.
$$

### Пример 6. Мажорантная теорема Лебега

На $[0,1]$ пусть

$$
f_n(x)=x^n.
$$

Тогда $f_n(x)\to0$ для $x\in[0,1)$ и $f_n(1)=1$. Значит $f_n\to0$ почти всюду. Кроме того,

$$
0\le f_n(x)\le1,
$$

а $g(x)=1$ интегрируема на $[0,1]$. Поэтому

$$
\int_0^1 x^n\,dx\to0.
$$

И действительно,

$$
\int_0^1 x^n\,dx=\frac1{n+1}.
$$

### Пример 7. Почему мажоранта нужна

На $[0,1]$ функции

$$
f_n(x)=n\mathbf 1_{(0,1/n)}(x)
$$

сходятся к $0$ почти всюду, но

$$
\int_0^1 f_n\,dx=1.
$$

Значит без интегрируемой мажоранты нельзя просто переносить предел под интеграл.

## 6. Доказательства и обоснования

**Почему простые функции являются первым шагом.**

Простая функция принимает конечное число значений, поэтому ее интеграл сводится к конечной сумме мер уровней. Это ровно тот случай, где мера множеств напрямую превращается в интеграл функции.

**Почему нужно приближение снизу.**

Для неотрицательной функции $f$ нельзя сразу сложить значения по всем уровням, потому что значений может быть бесконечно много. Вместо этого берут все простые функции $s$, которые лежат ниже $f$, и выбирают супремум их интегралов:

$$
\int f\,d\mu=\sup_{0\le s\le f}\int s\,d\mu.
$$

Так определение сохраняет монотонность и согласуется с интегралом простых функций.

**Почему интеграл может быть бесконечным.**

Для неотрицательных функций значение $\int f\,d\mu$ допускается в $[0,\infty]$. Например, на $\mathbb R$:

$$
\int_{\mathbb R}1\,dx=\infty.
$$

Это не ошибка, а часть конструкции. Конечность требуется только для интегрируемости в смысле $L^1$.

**Почему для знакопеременных функций нужна абсолютная интегрируемость.**

Если обе части $f^+$ и $f^-$ имеют бесконечный интеграл, выражение

$$
\int f^+\,d\mu-\int f^-\,d\mu
$$

имеет вид $\infty-\infty$ и не определено. Условие

$$
\int |f|\,d\mu<\infty
$$

гарантирует, что обе части конечны.

**Почему теорема Леви не требует мажоранты.**

В теореме Леви функции растут монотонно снизу. Интегралы не могут колебаться или терять массу: они тоже образуют возрастающую последовательность. Поэтому предел интегралов совпадает с интегралом предельной функции, возможно бесконечным.

**Почему теорема Лебега требует мажоранту.**

При обычной почти всюду сходимости графики могут становиться очень высокими на очень маленьких множествах. Точечный предел этого не видит, а интеграл видит площадь. Интегрируемая мажоранта $g$ запрещает такие пики и дает контроль:

$$
|f_n|\le g,\qquad \int g\,d\mu<\infty.
$$

**Что значит “без доказательства” в программе.**

Для теорем Беппо Леви и Лебега на экзамене достаточно дать точные условия и вывод, а также объяснить идею. Полные доказательства можно не разворачивать, если преподаватель специально не попросит.

## 7. Что написать на доске

1. Простая функция:

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k}.
$$

2. Интеграл простой неотрицательной функции:

$$
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

3. Интеграл неотрицательной функции:

$$
\int f\,d\mu
=
\sup_{0\le s\le f,\ s\text{ простая}}
\int s\,d\mu.
$$

4. Положительная и отрицательная части:

$$
f=f^+-f^-,
\qquad
|f|=f^++f^-.
$$

5. Интегрируемость:

$$
f\in L^1(\mu)
\quad\Longleftrightarrow\quad
\int |f|\,d\mu<\infty.
$$

6. Беппо Леви:

$$
0\le f_n\uparrow f
\quad\Longrightarrow\quad
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

7. Лебег:

$$
f_n\to f,\quad |f_n|\le g,\quad g\in L^1
\quad\Longrightarrow\quad
\int f_n\,d\mu\to\int f\,d\mu.
$$

## 8. Типичные дополнительные вопросы

**Вопрос. Что такое простая функция?**

Это измеримая функция, принимающая конечное число значений. Ее можно записать как

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
\qquad A_k\in\mathcal F.
$$

**Вопрос. Почему интеграл индикатора равен мере множества?**

Индикатор $\mathbf 1_A$ равен $1$ на $A$ и $0$ вне $A$, поэтому площадь под ним равна размеру множества $A$:

$$
\int\mathbf 1_A\,d\mu=\mu(A).
$$

**Вопрос. Как определяется интеграл неотрицательной измеримой функции?**

Через супремум интегралов простых минорант:

$$
\int f\,d\mu=\sup_{0\le s\le f}\int s\,d\mu.
$$

**Вопрос. Что говорит теорема Беппо Леви?**

Если $0\le f_n\uparrow f$, то

$$
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

**Вопрос. Чем теорема Леви отличается от теоремы Лебега?**

В теореме Леви нужна монотонность и неотрицательность, но не нужна мажоранта. В теореме Лебега монотонность не нужна, зато нужна интегрируемая мажоранта $g$.

**Вопрос. Что такое интегрируемая мажоранта?**

Это функция $g\in L^1(\mu)$ такая, что

$$
|f_n|\le g
$$

почти всюду для всех $n$.

**Вопрос. Почему нельзя применять теорему Лебега без мажоранты?**

Почти всюду сходимость не контролирует интегралы. Пример:

$$
f_n=n\mathbf 1_{(0,1/n)}\to0
$$

почти всюду, но

$$
\int_0^1 f_n\,dx=1.
$$

**Вопрос. Что значит $f\in L^1$?**

Это значит, что $f$ измерима и

$$
\int |f|\,d\mu<\infty.
$$

**Вопрос. Почему для знакопеременной функции нельзя просто интегрировать положительную и отрицательную части без условий?**

Потому что может возникнуть неопределенность $\infty-\infty$. Абсолютная интегрируемость исключает эту проблему.

## 9. Типичные ошибки

1. Применять мажорантную теорему Лебега, не предъявив $g\in L^1$.

2. Путать теорему Беппо Леви и теорему Лебега: в Леви нужна монотонность, в Лебеге - мажоранта.

3. Забывать, что для неотрицательной функции интеграл может быть равен $\infty$.

4. Для знакопеременной функции писать $\int f=\int f^+-\int f^-$, не проверив отсутствие неопределенности $\infty-\infty$.

5. Считать, что почти всюду сходимость сама по себе гарантирует сходимость интегралов.

6. Записывать интеграл простой функции без требования измеримости множеств $A_k$.

7. Забывать, что представление простой функции можно измельчать, и интеграл не зависит от конкретной записи.

## 10. Связи с другими билетами

**[[03|Билет 03]].** Интеграл Лебега строится по мере; мера Лебега получается как продолжение меры с полуалгебры.

**[[04|Билет 04]].** Для интеграла по $\mathbb R^d$ важны борелевские и лебегово измеримые множества.

**[[05|Билет 05]].** Интегрировать можно измеримые функции; измеримость - входное условие конструкции.

**[[06|Билет 06]].** Вероятности через плотности записываются как интегралы по мере Лебега.

**[[08|Билет 08]].** Характеристические функции и интегрируемость ограниченных измеримых функций опираются на интеграл Лебега.

**[[09|Билет 09]].** Теоремы Фубини и независимость используют интегралы по произведению мер.

**[[10|Билет 10]].** Теорема Радона-Никодима и условное ожидание формулируются через интегралы.

**[[11|Билет 11]].** Замена переменной, моменты и дисперсии выражаются через интеграл по вероятностному пространству и по распределению.

## 11. Минимум на удовлетворительно

Нужно уметь сказать:

1. Простая функция:

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k}.
$$

2. Интеграл простой функции:

$$
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

3. Интеграл неотрицательной функции:

$$
\int f\,d\mu=\sup_{0\le s\le f}\int s\,d\mu.
$$

4. Теорема Беппо Леви:

$$
0\le f_n\uparrow f
\Rightarrow
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

5. Теорема Лебега:

$$
f_n\to f,\quad |f_n|\le g\in L^1
\Rightarrow
\int f_n\,d\mu\to\int f\,d\mu.
$$

## 12. Минимум на хорошо

Кроме минимума на удовлетворительно, нужно:

1. Объяснить, почему интеграл простой функции не зависит от представления.
2. Назвать монотонность и линейность интеграла.
3. Рассказать про положительную и отрицательную части функции.
4. Объяснить, что такое $L^1$ и абсолютная интегрируемость.
5. Привести пример, где без мажоранты сходимость интегралов ломается.

## 13. Что добавить для отлично

Для сильного ответа стоит добавить:

1. Схему аппроксимации неотрицательной измеримой функции простыми функциями снизу.
2. Идею доказательства теоремы Беппо Леви через простую функцию $s\le f$ и множества $\{f_n\ge\alpha s\}$.
3. Идею мажорантной теоремы Лебега: мажоранта не дает массе уходить в пики.
4. Различие между конечным интегралом, бесконечным интегралом неотрицательной функции и интегрируемостью в $L^1$.
5. Связь с математическим ожиданием:

$$
\mathbb E X=\int X\,dP.
$$

## 14. Устная версия ответа на 5 минут

Интеграл Лебега строится по неотрицательной мере $(\Omega,\mathcal F,\mu)$. Начинают с индикатора:

$$
\int\mathbf 1_A\,d\mu=\mu(A).
$$

Потом берут простые функции. Простая функция - это измеримая функция, принимающая конечное число значений. Если

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
$$

где $a_k\ge0$, $A_k\in\mathcal F$ и $A_k$ попарно не пересекаются, то

$$
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

Корректность следует из перехода к общему измельчению двух разбиений.

Для произвольной неотрицательной измеримой функции $f$ интеграл определяется как супремум по простым функциям снизу:

$$
\int f\,d\mu
=
\sup_{0\le s\le f,\ s\text{ простая}}
\int s\,d\mu.
$$

Значение может быть бесконечным.

Для знакопеременной функции вводят

$$
f^+=\max(f,0),\qquad f^-=\max(-f,0).
$$

Если

$$
\int |f|\,d\mu<\infty,
$$

то $f\in L^1$ и

$$
\int f\,d\mu=\int f^+\,d\mu-\int f^-\,d\mu.
$$

Главные теоремы о предельном переходе идут в программе без доказательства. Теорема Беппо Леви: если

$$
0\le f_n\uparrow f,
$$

то

$$
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

Мажорантная теорема Лебега: если $f_n\to f$ почти всюду и есть $g\in L^1$ такая, что

$$
|f_n|\le g,
$$

то

$$
\int f_n\,d\mu\to\int f\,d\mu.
$$

Важно: почти всюду сходимости самой по себе недостаточно. Например, $f_n=n\mathbf 1_{(0,1/n)}$ сходится к нулю почти всюду на $[0,1]$, но интегралы равны $1$.

## 15. Если осталось 2 минуты перед экзаменом

Запомнить:

$$
s=\sum_{k=1}^n a_k\mathbf 1_{A_k},
\qquad
\int s\,d\mu=\sum_{k=1}^n a_k\mu(A_k).
$$

Для $f\ge0$:

$$
\int f\,d\mu
=
\sup_{0\le s\le f,\ s\text{ простая}}
\int s\,d\mu.
$$

Беппо Леви:

$$
0\le f_n\uparrow f
\Rightarrow
\int f_n\,d\mu\uparrow\int f\,d\mu.
$$

Лебег:

$$
f_n\to f,\quad |f_n|\le g,\quad g\in L^1
\Rightarrow
\int f_n\,d\mu\to\int f\,d\mu.
$$

Главная ловушка: теорема Лебега требует интегрируемую мажоранту, а просто почти всюду сходимости недостаточно.
